# Example Agentic use of GQL CLI

You have access to a local tool called `gql` via `npx gql`.

GQL CLI has been configured with an endpoint called `apollo`.

Explore the cli with `gql --help` to understand how to use it.